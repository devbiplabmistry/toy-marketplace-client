import { useLoaderData } from "react-router-dom";

const AllToy = () => {
    const addToy =useLoaderData()
    console.log(addToy);
    return (
        <div>
            
        </div>
    );
};

export default AllToy;